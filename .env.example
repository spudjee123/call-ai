# ============================================
# TWILIO
# ============================================
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+66xxxxxxxxx
# ตรวจ X-Twilio-Signature ของทุก POST /webhook/* — ค่า default คือ LOG_ONLY (log เตือนเฉยๆ ไม่บล็อกสายจริง
# เพราะ BASE_URL/proxy ที่ตั้งผิดจะทำให้ Twilio callback จริงถูกปฏิเสธไปด้วย) ตั้งเป็น ENFORCE เฉพาะหลังจากยืนยัน
# แล้วว่า LOG_ONLY ไม่เจอ warning เลยกับ production traffic จริงทั้งสอง service
WEBHOOK_SIGNATURE_MODE=LOG_ONLY

# ============================================
# ELEVENLABS
# ============================================
ELEVENLABS_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ELEVENLABS_VOICE_ID=xxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# ANTHROPIC (CLAUDE)
# ============================================
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# GOOGLE CLOUD (STT, TTS, Sheets)
# ============================================
# ใช้อย่างใดอย่างหนึ่ง:
# - GOOGLE_CREDENTIALS_JSON: วาง service account JSON ทั้งก้อนเป็น string เดียว (ใช้บน Render)
# - หรือปล่อยว่างไว้แล้วตั้ง GOOGLE_APPLICATION_CREDENTIALS ให้ชี้ไปที่ไฟล์ key (ใช้ตอน dev บนเครื่อง)
GOOGLE_CREDENTIALS_JSON=
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# ============================================
# DEEPGRAM (Dual STT Provider — optional, only needed if a campaign explicitly sets stt_provider=deepgram)
# ============================================
DEEPGRAM_API_KEY=

# ============================================
# GOOGLE SHEETS
# ============================================
GOOGLE_SHEETS_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# SERVER
# ============================================
PORT=3000
BASE_URL=https://your-app.onrender.com

# ============================================
# ADMIN CONTROL CENTER (/admin)
# ============================================
ADMIN_PASSWORD=change-me

# ============================================
# CALL SETTINGS
# ============================================
MAX_CONCURRENT_CALLS=5
MAX_CALL_DURATION_SECONDS=300

# ============================================
# RENDER (สำหรับ fetch-logs.js เท่านั้น — ไม่ใช้ตอน runtime)
# ============================================
RENDER_API_KEY=rnd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RENDER_SERVICE_ID=srv-xxxxxxxxxxxxxxxxxxxxxxxx
RENDER_OWNER_ID=tea-xxxxxxxxxxxxxxxxxxxxxxxx
